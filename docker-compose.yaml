version: '3'

services:
    db:
        image: postgres
        restart: always
        environment:
            POSTGRES_DB: database
            POSTGRES_USER: username
            POSTGRES_PASSWORD: password